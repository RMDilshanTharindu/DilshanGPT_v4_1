feat: upgrade DilshanGPT to v4.1 with improved embedding pipeline and vector store

- Integrated Google Generative AI embeddings (gemini-embedding-001)
- Added document loading using DirectoryLoader
- Implemented text chunking with CharacterTextSplitter
- Built persistent Chroma vector database with cosine similarity
- Structured modular pipeline (load → split → embed → store)
- Cleaned and prepared code for scalable RAG improvements


feat: add retrieval function to fetch relevant chunks from vector store

- Implemented retriever using Chroma vector store
- Configured top-k search (k=2) for relevant document retrieval
- Added query-based chunk fetching
- Printed retrieved document contents for debugging
- Integrated retrieval step into main pipeline


feat: add answer generation using Gemini LLM in RAG pipeline

- Implemented generate_answer function using Gemini (gemini-3-flash-preview)
- Constructed prompt with retrieved document chunks
- Enabled context-aware answer generation
- Added fallback response when information is insufficient
- Integrated full pipeline: retrieve → generate → respond



feat: add conversational memory and query rewriting to RAG pipeline

- Implemented chat_history to maintain conversation context
- Added generate_proper_question for follow-up query rewriting
- Integrated Gemini LLM for standalone query generation
- Built interactive CLI chat loop (start_chat)
- Enabled context-aware retrieval across multi-turn conversations



feat: implement hybrid retrieval using vector search and BM25

- Added keyword-based retrieval using BM25
- Combined semantic (vector) and keyword search results
- Implemented deduplication of retrieved documents
- Increased retrieval robustness with hybrid approach
- Improved relevance of retrieved chunks



feat: add multimodal document processing and AI-enhanced chunking to RAG pipeline

- Integrated PDF parsing using unstructured library
- Implemented title-based smart chunking for PDFs
- Added support for extracting tables and images from documents
- Built content-type separation (text, tables, images)
- Generated AI-enhanced summaries for complex chunks
- Converted enriched chunks into LangChain documents with metadata
- Combined text and PDF pipelines into unified vector store
- Improved retrieval quality with richer, searchable content


feat: optimize startup by loading existing vector DB

- Added check for existing Chroma DB directory
- Skip embedding and indexing if DB already exists
- Improved startup speed by going directly to chat mode



feat: implement persistent chunk storage and load existing vector database

- Save processed chunks using pickle
- Load chunks from local storage for BM25 retriever
- Load existing Chroma vector DB instead of rebuilding
- Improve startup performance by skipping ingestion if data exists